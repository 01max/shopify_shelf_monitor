name: Check Collections

on:
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch:
    inputs:
      force_notify:
        description: 'Force notification even if nothing changed'
        type: boolean
        default: false

jobs:
  check:
    runs-on: ubuntu-latest
    permissions:
      actions: write

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.4'
          bundler-cache: true

      - name: Download previous report
        uses: dawidd6/action-download-artifact@v20
        with:
          name: report-collections
          path: tmp
          if_no_artifact_found: ignore
        continue-on-error: true

      - name: Write config
        run: echo "$SHELF_MONITOR_CONFIG" > config.yml
        env:
          SHELF_MONITOR_CONFIG: ${{ secrets.SHELF_MONITOR_CONFIG }}

      - name: Run collection check
        run: bundle exec ruby check.rb
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_DEFAULT_CHAT_ID: ${{ secrets.TELEGRAM_DEFAULT_CHAT_ID }}
          FORCE_NOTIFY: ${{ inputs.force_notify }}
          CHECK_TYPE: collection

      - name: Upload report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: report-collections
          path: tmp/report-collections.json
          retention-days: 7

      - name: Disable workflow after 3 consecutive failures
        if: failure()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          recent=$(gh run list --workflow=check_collections.yml --limit=3 --json conclusion --jq '[.[].conclusion]')
          if [ "$recent" = '["failure","failure","failure"]' ]; then
            echo "3 consecutive failures — disabling workflow"
            gh workflow disable check_collections.yml
          fi
